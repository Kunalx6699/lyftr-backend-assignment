services:
  api:
    build: .
    ports:
      - "8000:8000"
    environment:
      DATABASE_URL: sqlite:////data/app.db
      WEBHOOK_SECRET: testsecret
      LOG_LEVEL: INFO
    volumes:
      - db_data:/data

volumes:
  db_data:

